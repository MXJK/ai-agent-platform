# Bundled Skills

Bundled declarative Skills normally live in one subdirectory per Skill with a
`SKILL.md` file. Discovery also supports frontmatter-bearing Markdown directly at
a Skill root, but ignores this `README.md`. It never executes scripts or loads code
from this directory.

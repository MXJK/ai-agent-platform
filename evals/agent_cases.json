{
  "repository_id": "repo_main",
  "fixtures": [
    {
      "filename": "ai_agent_platform/api/routes/agent_runs.py",
      "content": "def run_agent(request: AgentRunRequest) -> AgentRunStatusResponse:\n    \"\"\"Create an asynchronous agent run and return a run id.\"\"\"\n    return agent_run_service.submit_run(request)\n"
    },
    {
      "filename": "ai_agent_platform/agents/coding_agent.py",
      "content": "class CodingAgentRuntime:\n    \"\"\"LangGraph runtime for repository-aware coding tasks.\"\"\"\n\n    def resume(self, run_id: str, approved: bool, feedback: str | None = None):\n        \"\"\"Resume a waiting approval checkpoint.\"\"\"\n        return self._graph.invoke(Command(resume={\"approved\": approved, \"feedback\": feedback}))\n\n    def _review_tool_plan(self, state):\n        \"\"\"Pause the run until risky tools are approved.\"\"\"\n        return interrupt(_build_tool_plan_approval_request(state))\n"
    },
    {
      "filename": "ai_agent_platform/integrations/tools.py",
      "content": "class ToolRegistry:\n    \"\"\"Registry that validates tool arguments and audits execution.\"\"\"\n\n    def execute(self, tool_call: ToolCall, context: ToolExecutionContext | None = None) -> ToolResult:\n        validation_error = _validate_arguments(tool_call.arguments, spec.input_schema)\n        arguments_summary = summarize_tool_arguments(tool_call.arguments)\n        result, output_truncated = _truncate_output(result, max_chars=spec.max_output_chars)\n        return ToolResult(arguments_summary=arguments_summary, output_truncated=output_truncated)\n"
    }
  ],
  "cases": [
    {
      "id": "agent_locates_run_agent",
      "type": "agent",
      "message": "解释 run_agent 接口在哪里实现",
      "expected_status": "completed",
      "expected_intent": "repo_navigation",
      "expected_tools": ["repo.search_code", "repo.read_file", "code_explainer"],
      "expected_files": ["ai_agent_platform/api/routes/agent_runs.py"],
      "expected_answer_keywords": ["run_agent", "agent_runs.py"]
    },
    {
      "id": "agent_waits_for_change_approval",
      "type": "agent",
      "message": "帮我实现 agent 支持 repository_id 参数并补测试",
      "expected_status": "waiting_approval",
      "expected_intent": "change_planning",
      "expected_tools": ["change_planner", "test_designer"],
      "expected_pending_approval": true
    },
    {
      "id": "rag_finds_tool_registry_security",
      "type": "search",
      "query": "ToolRegistry execute 做了哪些安全校验和审计",
      "expected_files": ["ai_agent_platform/integrations/tools.py"],
      "expected_symbols": ["ToolRegistry"]
    },
    {
      "id": "agent_explains_resume_approval",
      "type": "agent",
      "message": "Agent run resume 审批在哪里处理？",
      "expected_status": "completed",
      "expected_intent": "repo_navigation",
      "expected_tools": ["repo.search_code"],
      "expected_files": ["ai_agent_platform/agents/coding_agent.py"],
      "expected_answer_keywords": ["resume", "coding_agent.py"]
    }
  ]
}
